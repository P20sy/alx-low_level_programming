#include "main.h"

/**
 * Main - Prints a string followed by a new line using recursion.
 * @s: The input string.
 */
void _puts_recursion(char *s)
{
    if (*s == '\0')  
    {
        _putchar('\n');  
        return;
    }
    
}

